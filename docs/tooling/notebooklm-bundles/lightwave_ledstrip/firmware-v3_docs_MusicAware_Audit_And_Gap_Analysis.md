# MusicAware Audit And Gap Analysis

GROUNDED

## Evidence And Scope

[FACT] This audit targets the current `firmware-v3` working tree on branch `feature/synergy-topology-resume-2026-05-05` at observed short commit `627b0b14`; Captain confirmed the current working tree as the inspection target. [FACT] `platformio.ini` sets the default environment to `esp32dev_audio_esv11_k1v2_32khz`, and that environment inherits the ESV11 K1v2 flags, then adds `FEATURE_AUDIO_BACKEND_ESV11_32KHZ=1` and force-includes `EsV11_32kHz_Shim.h` (`platformio.ini:16-17`, `platformio.ini:146-169`, `platformio.ini:176-204`, `platformio.ini:220-225`). [FACT] PipelineCore is a separate `FEATURE_AUDIO_BACKEND_PIPELINECORE=1` environment, not the reference production environment for this audit (`platformio.ini:241-249`).

[FACT] Reference evidence came from `/Users/spectrasynq/Workspace_Management/Software/Synesthesia/Docs`, `/Users/spectrasynq/Workspace_Management/Software/Tab5.DSP/docs/Auto_BPM_Technical_Analysis.md`, and `/Users/spectrasynq/Downloads/compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md`.

[FACT] Non-destructive branch/history checks found no committed `MusicAware`, `SongAware`, or `SongAwareDirector` source in current reachable refs, remote heads, stashes, or registered worktrees. [FACT] The current `SongAwareDirector` files and related handlers/tests/docs are untracked working-tree material, so claims about them are claims about this working tree, not about committed branch history.

[FALLBACK: clangd references returned empty for broad `SongAwareDirector` reference lookup] Consumer/call-site inventory used targeted `rg` discovery plus direct line-numbered source inspection. [FACT] clangd diagnostics and definitions were available for inspected source files, but broad reference enumeration was not used as proof of call edges.

## Part 1 - Current State Audit

### SongAwareDirector

[FACT] `src/core/songaware/` contains only `SongAwareDirector.h` and `SongAwareDirector.cpp` in the current working tree. `SongAwareDirector.h` defines the runtime language of the current director: modes `Off`, `Assist`, `Director`; profiles `Subtle`, `Balanced`, `High`; ownership, suppression, song-state, last-action, action-plan, intent, boundary-gate, switch-reason, and classification-reason enums (`src/core/songaware/SongAwareDirector.h:22-138`). [FACT] Its public status/config/output surface groups runtime enablement and profile controls, suppression and ownership state, raw/current/candidate song state, intent/action/boundary telemetry, confidence/selection score, decision counters and timers, selected effect metadata, transition telemetry, health counters, and audio summary values (`src/core/songaware/SongAwareDirector.h:142-218`, `src/core/songaware/SongAwareDirector.h:220-298`). [FACT] Its public methods expose singleton lifecycle, config/status/runtime snapshot, policy table/allowlist, manual/show ownership marks, and `FEATURE_AUDIO_SYNC`-guarded `evaluateDirector()`, transition notification, switch notification, and `apply()` methods (`src/core/songaware/SongAwareDirector.h:300-353`).

[FACT] The director stores its state as atomics grouped around config scalars, current/effective classification, action/intent/boundary telemetry, confidence and smoothed drive, counters/timers, effect IDs, audio summary, suppression windows, candidate-state hysteresis, switch-window state, transition telemetry, health recovery, and policy allowlist (`src/core/songaware/SongAwareDirector.h:413-493`). [FACT] It defines timing and safety constants for 0.18 s smoothing tau, 3000 ms boot grace, 4000 ms post-enable grace, 500 ms evaluation period, 400 ms stable hold, 250 ms drop hold, 8000 ms dwell, 20000 ms switch cooldown, 60000 ms switch window, two switches per window, 45000 ms anti-thrash, 3000 ms health clean, 15000 ms manual suppress, and 1000 ms show suppress (`src/core/songaware/SongAwareDirector.cpp:16-29`). [FACT] It also defines a static policy table that maps song states to target effect IDs, families, visual-language strings, switch reasons, and minimum confidence floors (`src/core/songaware/SongAwareDirector.cpp:31-50`).

[FACT] `evaluateDirector()` builds a feature snapshot from `ControlBusFrame` and `MusicalGridSnapshot`, records active effect and health counters, updates audio/transition/health telemetry, and then exits early for disabled/off mode, boot/enable grace, family morphing, non-Director mode, evaluation-period throttling, no audio, allowlist disabled, impossible transition, low confidence, unstable candidate, show/manual ownership, switching disabled, degraded/recovering health, invalid/same target, active transition, ABA anti-thrash, dwell, cooldown, rate limit, or missing beat/downbeat/phase boundary (`src/core/songaware/SongAwareDirector.cpp:533-745`). [FACT] Only after those gates pass does it populate `SongAwareSwitchRequest` with target effect, family, visual language, and reason (`src/core/songaware/SongAwareDirector.cpp:735-745`).

[FACT] `apply()` is the parameter-modulation path: it consumes `ControlBusFrame`, `MusicalGridSnapshot`, audio availability, delta time, current time, and mutable `SongAwareParams`; it gates disabled/off, grace, family morphing, no audio, confidence floor, and silence/unknown state, then classifies state, resolves an action plan, smooths a drive scalar with `1 - exp(-dt/tau)`, and mutates speed, intensity, complexity, saturation, variation, and hue (`src/core/songaware/SongAwareDirector.cpp:811-971`). [FACT] The classification path uses `frame.fast_rms`, `frame.rms`, `fast_flux`, `flux`, `onsetEvent`, `es_beat_strength`, `tempoBeatStrength`, grid beat strength, `es_tempo_confidence`, `tempoConfidence`, liveliness, saliency, adaptive floor, and current stable state to produce Silence, Drop, Transition, Breakdown, Dense, Build, Ambient, or Steady (`src/core/songaware/SongAwareDirector.cpp:974-1110`). [FACT] Candidate-state hysteresis requires the raw state to persist for 400 ms, or 250 ms for high-confidence Drop, before stable-state change (`src/core/songaware/SongAwareDirector.cpp:1113-1174`).

### Production Audio Surface

[FACT] The production reference path is `AudioActor -> EsV11Backend -> EsV11Adapter -> ControlBusFrame -> SnapshotBuffer -> RendererActor/EffectContext`: `AudioActor` includes the ESV11 backend under `FEATURE_AUDIO_BACKEND_ESV11`, owns `EsV11Backend`, `EsV11Adapter`, and a `ControlBusFrame` snapshot buffer, initialises ESV11, builds frames through the adapter, and publishes frames by value (`src/audio/AudioActor.h:49-52`, `src/audio/AudioActor.h:646-658`, `src/audio/AudioActor.cpp:610-638`, `src/audio/AudioActor.cpp:727-737`, `src/audio/AudioActor.cpp:1121-1133`). [FACT] The ESV11 32 kHz path uses 32000 Hz and 256-hop production cadence: the shim defines 32000 sample rate and 128-sample chunking, config defines 32000 sample rate, 256 hop, and 128 chunk size, and `AudioActor` publishes every two 128-sample chunks (`src/audio/backends/esv11/EsV11_32kHz_Shim.h:19-20`, `src/config/audio_config.h:113-118`, `src/audio/AudioActor.cpp:713-721`). [FACT] Renderer target cadence remains 120 FPS / 8333 us (`src/core/actors/RendererActor.h:114-115`).

[FACT] `ControlBusFrame` exposes timing, RMS/flux and fast RMS/flux, liveliness, scene parameters, 8 bands, 12 chroma, heavy-smoothed bands/chroma, waveform, Sensory Bridge parity fields, chord/saliency/style, onset and percussion fields, optional HF semantics, 64-bin and 256-bin spectra, STM fields, tempo fields, ESV11 tempo extras, ESV11 raw signals, silence/audio-confidence fields, spectral novelty, and motion-semantic fields (`src/audio/contracts/ControlBus.h:118-259`). [FACT] ESV11 adapter populates energy, spectrum, chroma, raw ES VU, 64 bins, adaptive 64 bins, 8 bands, 12 chroma, heavy bands/chroma, waveform, SB side-car fields, snare/hihat energy, optional HF semantics, and ESV11 tempo extras (`src/audio/backends/esv11/EsV11Adapter.cpp:68-181`, `src/audio/backends/esv11/EsV11Adapter.cpp:183-341`, `src/audio/backends/esv11/EsV11Adapter.cpp:343-433`).

[FACT] `EffectContext` exposes effect-facing accessors for RMS/flux, bands and heavy bands, bass/mid/treble, beat phase, beat/downbeat booleans, BPM, tempo confidence, audio confidence, beat tick, beat-in-bar, beat strength, scene parameters and phrase progress, chroma, hop sequence, liveliness, silence, onset envelope/event/flux, kick/snare/hihat levels, 64-bin musical bins/ranges, and PipelineCore-only 256-bin named-band accessors (`src/plugins/api/EffectContext.h:94-179`, `src/plugins/api/EffectContext.h:185-190`, `src/plugins/api/EffectContext.h:273-427`, `src/plugins/api/EffectContext.h:429-487`). [FACT] In non-PipelineCore builds, including the ESV11 production build, `subBass()`, `kick()`, `lowMid()`, `midPresence()`, `shimmer()`, and `air()` fall back to legacy bass/mid/treble accessors (`src/plugins/api/EffectContext.h:479-487`).

### Existing Beat, Onset, Tempo, Phase, And Structure Logic

[FACT] Production tempo/beat tracking is ESV11. `EsV11Backend` exports `top_bpm`, `tempo_confidence`, `phase_radians`, `beat_tick`, and `beat_strength` (`src/audio/backends/esv11/EsV11Backend.h:22-40`). [FACT] The backend initialises tempo constants, processes chunk-level novelty/tempo updates, and refreshes outputs (`src/audio/backends/esv11/EsV11Backend.cpp:65-70`, `src/audio/backends/esv11/EsV11Backend.cpp:89-120`, `src/audio/backends/esv11/EsV11Backend.cpp:176-183`). [FACT] The vendored ESV11 tempo bank uses `NUM_TEMPI=96`, `TEMPO_LOW=48`, `NOVELTY_LOG_HZ=50`, and 1024 novelty history (`src/audio/backends/esv11/vendor/global_defines.h:20-35`); its novelty path is positive spectral delta logged at 50 Hz, `update_tempo()` calculates two tempo bins per call, `update_tempi_phase()` smooths magnitudes and derives confidence, and octave-aware selection handles sub-80 BPM doubling and alias guards (`src/audio/backends/esv11/vendor/tempo.h:161-182`, `src/audio/backends/esv11/vendor/tempo.h:239-305`, `src/audio/backends/esv11/vendor/tempo.h:372-442`).

[FACT] Production render-domain phase tracking uses `EsBeatClock`, not the `MusicalGrid.cpp` implementation. `RendererActor` owns `EsBeatClock` under ESV11 and `MusicalGrid` only otherwise, ticks `m_esBeatClock` on the ESV11 render path, and publishes `MusicalGridSnapshot` for effect-facing beat/bar state (`src/core/actors/RendererActor.h:1030-1042`, `src/core/actors/RendererActor.cpp:1936-1939`, `src/audio/backends/esv11/EsBeatClock.h:5-8`, `src/audio/backends/esv11/EsBeatClock.cpp:79-95`, `src/audio/backends/esv11/EsBeatClock.cpp:116-197`). [FACT] `MusicalGrid.cpp` still implements a render-domain PLL with tempo observations, beat/downbeat correction, bar phase, and tick generation, but ESV11 excludes `audio/contracts/MusicalGrid.cpp` and `audio/tempo/TempoTracker.cpp` from the reference build (`src/audio/contracts/MusicalGrid.cpp:61-84`, `src/audio/contracts/MusicalGrid.cpp:179-330`, `platformio.ini:156-169`).

[FACT] Production onset detection has two layers. `OnsetDetector` is a 1024-point FFT log-spectral-flux detector with warmup, adaptive threshold, peak picker, and kick/snare/hihat band ranges; `AudioActor` initialises it and runs it when 1024 samples are available, but the FFT onset triggers are explicitly telemetry-only and are not the live trigger source (`src/audio/onset/OnsetDetector.h:70-154`, `src/audio/AudioActor.cpp:632-634`, `src/audio/AudioActor.cpp:824-879`). [FACT] Live production percussion triggers come from the band-energy ratio detector in `AudioActor`, which groups kick/snare/hihat energies, applies RMS gate hold and adaptive ratio detection, then sets `kickTrigger`, `snareTrigger`, `hihatTrigger`, and `onsetEvent` (`src/audio/AudioActor.h:674-676`, `src/audio/AudioActor.cpp:883-933`).

[FACT] Non-production/excluded tempo components still exist. `TempoTracker` implements 96 tempo bins, novelty history, winner selection, hysteresis, phase advancement, beat tick, and lock threshold, but is excluded from ESV11 (`src/audio/tempo/TempoTracker.h:43-85`, `src/audio/tempo/TempoTracker.cpp:397-489`, `src/audio/tempo/TempoTracker.cpp:544-687`, `platformio.ini:156-169`). [FACT] PipelineCore has `BeatTracker` and a `FeatureFrame` surface with tempo BPM, confidence, locked flag, beat phase, and beat event, but PipelineCore is a separate backend environment and current PipelineCore comments say the old comb/CBSS beat tracker was replaced there by a TempoTracker compatibility path (`src/audio/pipeline/BeatTracker.h:6-48`, `src/audio/pipeline/BeatTracker.cpp:117-360`, `src/audio/pipeline/PipelineCore.h:68-99`, `src/audio/pipeline/PipelineCore.cpp:528-567`, `platformio.ini:241-249`).

[FACT] Phrase/bar/section-like logic exists in `TranslationEngine`, not as a full structure analyser. It latches reliable tempo after confidence/consecutive-hop thresholds, keeps `beat_in_bar`, `bar_in_phrase`, 4/8 bar phrase length, phrase progress, and phrase boundary from mean-delta/chord-root-change heuristics; `AudioActor` invokes translation in the ESV11 path (`src/audio/TranslationEngine.cpp:22-28`, `src/audio/TranslationEngine.cpp:209-245`, `src/audio/TranslationEngine.cpp:264-335`, `src/audio/AudioActor.cpp:1043-1067`).

### Kuramoto And Downstream Consumers

[FACT] `KuramotoTransportEffect` has no direct `SongAwareDirector` dependency in the inspected implementation. It consumes the already-mutated `EffectContext`: raw dt from `AudioReactivePolicy::signalDt(ctx)`, speed-scaled dt from `ctx.getSafeDeltaSeconds()`, scene translator values when enabled, audio saliency/flux, intensity, hue, palette, mood, and brightness (`src/effects/ieffect/KuramotoTransportEffect.cpp:125`, `src/effects/ieffect/KuramotoTransportEffect.cpp:134`, `src/effects/ieffect/KuramotoTransportEffect.cpp:144`, `src/effects/ieffect/KuramotoTransportEffect.cpp:181`, `src/effects/ieffect/KuramotoTransportEffect.cpp:212`, `src/effects/ieffect/KuramotoTransportEffect.cpp:217`). [FACT] The oscillator field is stepped in `render()` with zone ID, raw dt, coupling `K`, spread, radius, noise sigma, kick rate, and kick strength derived in the same frame (`src/effects/ieffect/KuramotoTransportEffect.cpp:150-167`, `src/effects/ieffect/KuramotoOscillatorField.h:117-189`).

[FACT] The direct firmware consumer of `SongAwareDirector::apply()` is `RendererActor`: it calls `apply()` before speed-scaled timing is computed, writes returned values into the shared render context, then computes `deltaTimeSeconds`/`deltaTimeMs` and calls `effect->render(ctx)` (`src/core/actors/RendererActor.cpp:2273`, `src/core/actors/RendererActor.cpp:2284`, `src/core/actors/RendererActor.cpp:2292`, `src/core/actors/RendererActor.cpp:2328`, `src/core/actors/RendererActor.cpp:2346`, `src/core/actors/RendererActor.cpp:2351`). [FACT] The Director switching path is separate: `RendererActor` calls `evaluateDirector()` with audio/grid availability, health counters, and the active effect, and transition execution reports applied/rejected/started/completed state back into the director (`src/core/actors/RendererActor.cpp:2130-2144`, `src/core/actors/RendererActor.cpp:1619-1703`, `src/core/songaware/SongAwareDirector.cpp:533-745`).

[FACT] REST contract consumers are `/api/v1/songAware/config`, `/status`, `/allowlist`, and `/allowlist/reset` for `[ios, tab5, lab]`, routed through `SongAwareHandlers` (`docs/protocol/k1-rest-contract.yaml:169`, `docs/protocol/k1-rest-contract.yaml:213`, `docs/protocol/k1-rest-contract.yaml:284`, `docs/protocol/k1-rest-contract.yaml:303`, `src/network/webserver/V1ApiRoutes.cpp:262`, `src/network/webserver/V1ApiRoutes.cpp:291`, `src/network/webserver/V1ApiRoutes.cpp:297`). [FACT] WS contract consumers include `songAware.config.get/set`, `status`, `reset`, `restore`, `debug`, `policy`, `allowlist`, `allowlist.set/reset`, `health`, and `counters.reset`, registered by `WsSongAwareCommands` (`docs/protocol/k1-ws-contract.yaml:296`, `docs/protocol/k1-ws-contract.yaml:346`, `docs/protocol/k1-ws-contract.yaml:419`, `docs/protocol/k1-ws-contract.yaml:443`, `docs/protocol/k1-ws-contract.yaml:478`, `docs/protocol/k1-ws-contract.yaml:516`, `docs/protocol/k1-ws-contract.yaml:533`, `src/network/webserver/ws/WsSongAwareCommands.cpp:386`). [FACT] Text serial and serial JSON expose analogous status/config/debug/allowlist/mode/profile/reset/counter controls (`src/serial/SerialCLI.cpp:104`, `src/serial/SerialCLI.cpp:159`, `src/serial/SerialCLI.cpp:191`, `src/serial/SerialCLI.cpp:728`, `src/serial/SerialCLI.cpp:761`, `src/serial/SerialCLI.cpp:816`, `src/serial/SerialCLI.cpp:845`, `src/serial/SerialCLI.cpp:891`, `src/serial/SerialJsonGateway.cpp:847`, `src/serial/SerialJsonGateway.cpp:850`, `src/serial/SerialJsonGateway.cpp:857`, `src/serial/SerialJsonGateway.cpp:872`, `src/serial/SerialJsonGateway.cpp:904`, `src/serial/SerialJsonGateway.cpp:935`, `src/serial/SerialJsonGateway.cpp:966`).

## Part 2 - Reference Architecture Extraction

| Parameter | Synesthesia - Tier 1 | AutoBPM - Tier 1 | Academic Consensus - Tier 2 |
|---|---|---|---|
| Front-end | [FACT] 44.1/48 kHz, mono preferred, 256-sample typical buffer, 2048 FFT, 1024 hop, Hann, vDSP real FFT, 1024 magnitude bins, spectral flux `sum max(0, delta magnitude)`, Mel-weighted around 100-300 Hz, threshold `median * 1.5 + std * 0.5`, 150 ms minimum onset interval (`Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:15-23`, `:46-70`; `Synesthesia.RE/02_ALGORITHMS/04_FFT_Processing.md:13-21`, `:137-165`). | [FACT] Sample rate 44100 is inferred by the doc, FFT 2048 with 2048/4096 options, hop 512 standard/inferred and configuration-dependent, Hanning window, LogComplexDomainNoveltyGenerator combining phase and magnitude novelty, log-magnitude spectral flux, and complex-domain phase-deviation components (`Auto_BPM_Technical_Analysis.md:462-468`, `:657-739`, `:2440-2444`, `:2513-2534`). | [FACT] Dominant causal recipe is complex spectral difference at 44.1 kHz, 1024 frame, 512 hop, Hann, 11.6 ms hop. Disagreements: aubio flux/complex/HFC, Ellis 8 kHz/256/4 ms, Goto 22.05 kHz/1024/256, Gkiokas 16 kHz/1024/160 (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:3-19`, `:58-65`, `:77-87`, `:112-115`, `:129`). |
| Tempo induction | [FACT] IOI method: BPM `60.0 / IOI`, valid range 60-200 BPM, median IOI over 10 recent onsets, 200-500 ms convergence; registry names `Algorithm_BPM` as leaky histogram with logistic confidence and phase correction; example accumulates at least 20 pulses, uses autocorrelation on inter-beat intervals, IBI histogram, and `intervalHistogram[180]` for 60-180 BPM (`Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:72-83`; `Synesthesia.RE/06_REFERENCE/01_Algorithm_Parameters.md:36-42`, `:89-97`; `Synesthesia.RE/01_FOUNDATIONS/02_System_Architecture.md:671-676`, `:718-726`). | [FACT] BPM range 20-200, lag range 21-258 for 44100/512, ACF via FFT/Wiener-Khinchin, candidate generation over `[minLag,maxLag]`, candidate scoring by pulse-train correlation with optional `P/2` and `P/3` harmonics, musical priors 100-140 central, pop about 120, EDM about 128, hip-hop about 90 (`Auto_BPM_Technical_Analysis.md:872-986`, `:1287-1310`, `:2284-2288`, `:2450-2453`). | [FACT] Dominant range 80-160 BPM, six-second autocorrelation, Rayleigh prior near 120 BPM, comb filterbank/observation vectors. Disagreements: Rayleigh beta 48 versus 43, aubio about 40-250 with folding, Goto 61-185, Gkiokas 85.7-171, Ellis log-Gaussian prior and divergent tightness values (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:9-28`, `:39-47`, `:58-69`, `:77-87`, `:112-116`, `:167`, `:244-246`). |
| Lock criterion | [FACT] NOT SPECIFIED as explicit lock/demotion. Control extraction states no explicit locked state, only confidence, with no hysteresis or hold timer; separate spec says 200-500 ms convergence but no predicate (`synesthesia/beat_tracker_control_extraction.md:164-168`; `Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:81-83`). | [FACT] NOT SPECIFIED. | [FACT] BTrack has no published lock/confidence flag; Davies & Plumbley lock after 3 consecutive consistent beat-period observations; aubio has `g_var=3.901`; IBT 5 s induction and kills an agent after 8 consecutive bad predictions; Goto uses continuous reliability without hard lock (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:28-31`, `:44-50`, `:97-104`, `:135-144`). |
| Tolerance window | [FACT] Beat-alignment tolerance NOT SPECIFIED. Refractory onset interval is 150 ms; control extraction maps this to about 5-6 hops / about 120 ms depending on version (`Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:58-70`; `synesthesia/beat_tracker_control_extraction.md:75-85`). | [FACT] Candidate picker filters candidates within a tolerance of higher-scoring candidates, but numeric tolerance is NOT SPECIFIED (`Auto_BPM_Technical_Analysis.md:988-999`). | [FACT] Disagreements: +/-70 ms fixed, +/-17.5% inter-annotation interval, +/-20% beat period, +/-15% IBI, +/-46.4 ms IBT inner tolerance plus asymmetric outer, sigma 40 ms Cemgil, onset windows +/-50/70 ms (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:146-159`). |
| Confidence scoring | [FACT] Onset confidence is `flux / threshold` or clipped 0-1. Beat confidence variants include `0.4 * onset_strength + 0.3 * tempo_consistency + 0.2 * stability_score + 0.1 * cross_validation` with safe/conservative/no-trigger bands, and BPM confidence as valid IOI count divided by IOI history length (`Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:62-69`; `Synesthesia.RE/02_ALGORITHMS/01_Beat_Detection_Complete.md:278-308`; `synesthesia/beat_tracker_control_extraction.md:113-128`). | [FACT] Per-band confidence weighting is described; beat strength is exponential-smoothed confidence; `_showConfidence` exists. No numeric confidence threshold is specified (`Auto_BPM_Technical_Analysis.md:1094-1107`, `:1359-1388`, `:1637-1644`). | [FACT] Confidence patterns include counters/reliability score, probabilistic state, exponential decay/inheritance, and discrete state machine; BTrack publishes no confidence scheme and no reviewed paper gives a direct-port numerical threshold (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:161-163`). |
| Octave-error handling | [FACT] Control extraction multiplies/divides IOI-derived BPM by 2 when outside `BPM_MIN`/`BPM_MAX` and warns of x2/x0.5 oscillation near boundaries; separate spec says out-of-range behaviour clamps to nearest valid value, so Synesthesia docs conflict (`synesthesia/beat_tracker_control_extraction.md:87-98`; `Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:76-83`). | [FACT] OctaveCorrector folds estimates into range by multiplying/dividing by octave size 2.0; OctaveScorer uses range preference, harmonic consistency, musical priors, temporal consistency, and classifier features (`Auto_BPM_Technical_Analysis.md:1244-1322`). | [FACT] Mechanisms include prevention by prior, Smith Beat Critic threshold `e > 3.34`, joint-template inference, external classifier, and explicit ambiguity for bimodal excerpts (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:169-181`). |
| Re-acquisition after coast | [FACT] NOT SPECIFIED as coast/reacquire. Related but not equivalent: silence false-positive rejection expects stable default/no lock, threshold adapts within 1-2 s after level change, and discretisation doc defines a model metric for reacquire after tempo step (`Synesthesia.RE/02_ALGORITHMS/05_Algorithm_Specifications.md:520-535`, `:600-615`; `synesthesia/beat_tracker_discretization.md:274-277`). | [FACT] NOT SPECIFIED. | [FACT] Only qualitative recovery appears in IBT state-recovery monitor and B-Keeper; B-Keeper slews phase over the next few beats with separate P/I gains; no numerical silence-return validation threshold (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:183`, `:242`). |
| Downbeat / phrase grid | [FACT] Downbeat grid NOT SPECIFIED; phrase grid NOT SPECIFIED. | [FACT] Downbeat extraction NOT SPECIFIED. Phrase/bar constants appear only as octave-related multipliers for 2 bars at 3/4, standard bars, 4 bars, 5 bars, 8 bars; no phrase-grid extractor is specified (`Auto_BPM_Technical_Analysis.md:2413-2428`). | [FACT] Hockman/Davies/Fujinaga downbeat uses 40 ERB sub-bands, lowest 5 ERB bands, bar period `4 * median IBI`, beat-time weighting 1.3, and 6.25% tolerance; Goto uses chord-change plus drum patterns; BeatNet supports causal joint beat/downbeat/meter. Phrase grids 4/8/16/32 bars are conventions, not explicit causal extraction algorithms (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:185-197`, `:250`). |
| Structural segmentation | [FACT] Only `PrimeAndBangStructureDetectorComponent` / `syn_PrimeBang` is identified, likely for phrase/drop/build boundaries; no parameters are specified (`Synesthesia.RE/03_ARCHITECTURE/01_Pipeline_Execution_Flow.md:620-628`, `:1495-1499`). | [FACT] NOT SPECIFIED for musical structure; only generic audio reader segmenter fields are listed (`Auto_BPM_Technical_Analysis.md:395-412`). | [FACT] Foote novelty kernel is offline reference with 0.5 s, 2 s, 8 s kernels; Yadati EDM drop detection uses chroma 209 ms window / 139 ms hop, 2.5 s boundary error, MFCC/rhythm features, F1 values, and 30 s refractory. No explicit RMS-drop, flatness, or band-energy thresholds are published (`compass_artifact_wf-541d094f-3147-473a-bd72-375095a834de_text_markdown.md:199-207`, `:252`). |

## Part 3 - Gap Matrix

| Requirement | Current firmware state | Reference architecture target |
|---|---|---|
| 1. Causal onset detection function | PARTIAL - production has band-ratio live triggers and FFT onset telemetry; see `AudioActor.cpp:824-933`, `AudioActor.h:674-676`, `OnsetDetector.h:70-154`. | [FACT] Tier 1 Synesthesia specifies spectral flux with 2048 FFT/1024 hop/Hann, adaptive median/std threshold, 150 ms refractory; AutoBPM specifies log-complex novelty with phase and magnitude components; Tier 2 prefers complex spectral difference. |
| 2. Tempo induction (autocorrelation or comb-bank) | PARTIAL - production ESV11 tempo bank has 96 tempo bins and novelty/phase update; PipelineCore/TempoTracker alternatives exist but are not production; see `vendor/global_defines.h:20-35`, `vendor/tempo.h:161-442`, `platformio.ini:156-169`, `PipelineCore.cpp:528-567`. | [FACT] AutoBPM specifies ACF via Wiener-Khinchin and pulse-train scoring over 20-200 BPM; Tier 2 specifies six-second autocorrelation/comb filterbank; Synesthesia specifies IOI median/histogram rather than a named comb-bank. |
| 3. Tempo prior (shape, peak, width) | PARTIAL - production has ESV11 tempo-bin range and octave/alias guards but no documented Rayleigh/log-Gaussian prior surface; see `vendor/global_defines.h:20-35`, `vendor/tempo.h:372-442`. | [FACT] AutoBPM gives musical priors around 100-140, pop 120, EDM 128, hip-hop 90; Tier 2 gives Rayleigh near 120 with beta disagreement 43 vs 48 and Ellis log-Gaussian disagreement. |
| 4. Tempo lock criterion | PARTIAL - production exposes tempo confidence and ESV11 stable bin validation, but no MusicAware master-lock state; `TempoTracker` lock exists outside ESV11; see `EsV11Backend.h:34-40`, `EsV11Backend.h:80-95`, `TempoTracker.h:43-85`, `platformio.ini:156-169`. | [FACT] Tier 1 docs do not specify hard lock/demotion. Tier 2 offers candidates: Davies 3 consecutive consistent beat-period observations, IBT 5 s induction and 8 bad predictions, Goto continuous reliability. |
| 5. Beat-phase flywheel (prediction, coast, re-sync) | PARTIAL - `EsBeatClock` integrates phase at render cadence, resynchronises on audio frames, wraps beat phase, and emits beat/downbeat ticks, but no documented silence-coast validation state machine; see `EsBeatClock.cpp:79-197`. | [FACT] Tier 2 B-Keeper qualitatively slews phase over next beats; academic docs do not provide numerical silence re-validation thresholds. MusicAware brief requires flywheel coast through silence. |
| 6. Beat tolerance window | PARTIAL - production boundary logic uses grid tick/downbeat/phase fallback thresholds, not a documented IBI/ms tolerance window; see `SongAwareDirector.cpp:1019-1047`. | [FACT] Tier 2 windows disagree: +/-70 ms, +/-17.5%, +/-20%, +/-15%, +/-46.4 ms, sigma 40 ms. Synesthesia/AutoBPM do not specify a beat-alignment tolerance. |
| 7. Confidence scoring | PARTIAL - production uses `audioConfidence`, `es_tempo_confidence`, `tempoConfidence`, beat strength, selection score, policy min confidence, and director confidence floors, but not a single master confidence model; see `ControlBus.h:207-224`, `SongAwareDirector.h:149-153`, `SongAwareDirector.cpp:599-620`, `SongAwareDirector.cpp:1397-1420`. | [FACT] Synesthesia specifies several conflicting confidence forms; AutoBPM has per-band/exponential-smoothed confidence but no threshold; Tier 2 gives patterns without direct thresholds. |
| 8. Octave-error handling | PARTIAL - ESV11 selection handles sub-80 BPM doubling and alias guards; AutoBPM-like octave scorer is absent; see `vendor/tempo.h:372-442`. | [FACT] AutoBPM specifies octave fold/correct plus scoring by range, harmonic consistency, priors, and temporal consistency; Tier 2 offers prevention by prior and post-hoc correction. |
| 9. Coast-through-silence behaviour | PARTIAL - current director classifies silence and suppresses switching; `EsBeatClock` phase integration may continue at render cadence, but no explicit locked flywheel coast/revalidate API is exposed; see `SongAwareDirector.cpp:1057-1075`, `EsBeatClock.cpp:116-197`. | [FACT] MusicAware brief requires flywheel coast through silence. Tier 1 and Tier 2 references do not specify full numerical behaviour. |
| 10. Re-validation on audio return | ABSENT - no current source exposes a MusicAware re-validation threshold after silence return. | [FACT] Tier 1 AutoBPM/Synesthesia do not specify it; Tier 2 only qualitative IBT/B-Keeper recovery. Captain adjudication required. |
| 11. Downbeat detection | PARTIAL - production has `es_downbeat_tick`, `tempoDownbeatTick`, `MusicalGridSnapshot.downbeat_tick`, and `EsBeatClock` downbeat tick, but no reference-backed downbeat detector using bass/chroma/drum patterns; see `ControlBus.h:207-224`, `MusicalGrid.h:22-33`, `EsBeatClock.cpp:116-197`. | [FACT] Tier 2 specifies downbeat candidates from Hockman/Davies/Fujinaga, Goto chord/drum patterns, and BeatNet; Tier 1 docs mostly NOT SPECIFIED. |
| 12. Phrase-grid (8/16/32 bar) extraction | PARTIAL - `TranslationEngine` tracks 4/8 bar phrase length, `bar_in_phrase`, phrase progress, and phrase boundary heuristics; no 16/32 bar extraction exists; see `TranslationEngine.cpp:22-28`, `TranslationEngine.cpp:209-335`. | [FACT] References do not provide causal 8/16/32 bar extractor. AutoBPM only lists phrase-like octave multipliers; Tier 2 treats phrase grids as convention. |
| 13. Structural-section detection (verse/chorus/breakdown/build) | PARTIAL - `SongAwareDirector` classifies Silence/Ambient/Steady/Build/Drop/Breakdown/Dense/Transition from frame-level thresholds, and `TranslationEngine` has phrase-boundary heuristics, but no slow section detector with verse/chorus state; see `SongAwareDirector.h:67-77`, `SongAwareDirector.cpp:1050-1110`, `TranslationEngine.cpp:264-335`. | [FACT] Synesthesia exposes `syn_PrimeBang` without parameters; Tier 2 structural references are offline or EDM-drop-focused and do not give K1-ready thresholds. |
| 14. Aesthetic intent surface (handoff to Kuramoto) | PARTIAL - `SongAwareDirector` exposes intent/action plan and mutates `EffectContext` controls before render; Kuramoto consumes `EffectContext` values, not direct intent; see `SongAwareDirector.h:96-105`, `SongAwareDirector.cpp:1194-1244`, `RendererActor.cpp:2273-2351`, `KuramotoTransportEffect.cpp:125-217`. | [FACT] MusicAware brief requires aesthetic intent as an orthogonal state machine and Kuramoto parameter surface. Tier 1/Tier 2 references do not specify Kuramoto-specific handoff parameters. |
| 15. Master phase reference exposure (public API consumed by other subsystems) | PARTIAL - current public surfaces expose BPM, beat phase, beat/downbeat ticks, beat-in-bar, tempo confidence through `ControlBusFrame`, `MusicalGridSnapshot`, `OnsetContext`, and `EffectContext`; no named master phase lock API exists; see `ControlBus.h:207-224`, `MusicalGrid.h:12-34`, `OnsetContext.h:7-40`, `EffectContext.h:134-179`. | [FACT] MusicAware brief requires master phase lock as anchor, exposed to Kuramoto, structure, and effect parameter surface. References provide beat/tempo mechanisms but not this K1 public API. |

### Partial Row Survival Notes

- Requirement 1:
  - What survives: existing `OnsetDetector` telemetry fields, band-ratio live trigger plumbing, `OnsetContext`, and effect accessors.
  - What dies: treating ad hoc band-ratio triggers as the only causal onset truth for MusicAware.
  - What changes: choose one reference-backed causal onset function and declare whether FFT onset telemetry becomes authoritative or remains side telemetry.

- Requirement 2:
  - What survives: ESV11 novelty/tempo bank evidence and PipelineCore/TempoTracker code as reference material.
  - What dies: assuming PipelineCore comb/CBSS is production just because it exists.
  - What changes: MusicAware needs one induction path explicitly tied to the production build surface.

- Requirement 3:
  - What survives: current tempo-bin constraints and octave/alias guard evidence.
  - What dies: implicit prior behaviour hidden inside legacy code.
  - What changes: the prior shape, centre, and width must become explicit specification parameters.

- Requirement 4:
  - What survives: existing confidence fields and candidate/stability ideas from ESV11/TempoTracker.
  - What dies: confidence-only behaviour without a named master-lock state.
  - What changes: lock promotion/demotion must be specified as a first-class state transition.

- Requirement 5:
  - What survives: `EsBeatClock` render-domain phase integration and tick publication.
  - What dies: reacquiring phase from frame-local evidence without a master flywheel contract.
  - What changes: phase prediction, coast, and cheap resync need explicit state and public outputs.

- Requirement 6:
  - What survives: current boundary gating and phase fallback concepts.
  - What dies: hidden or state-local beat-boundary thresholds as the only tolerance model.
  - What changes: tolerance must be expressed as fixed ms, percent IBI, or a Captain-approved hybrid.

- Requirement 7:
  - What survives: `audioConfidence`, tempo confidence, beat strength, policy confidence floors, and score surfaces.
  - What dies: overlapping confidence meanings with no single confidence contract.
  - What changes: define which confidence belongs to onset, tempo lock, phase, structure, and aesthetic intent.

- Requirement 8:
  - What survives: ESV11 octave/alias guards and AutoBPM reference scoring concepts.
  - What dies: octave fixes that only fold out-of-range BPM without post-hoc confidence accounting.
  - What changes: octave prevention and correction need one declared decision path.

- Requirement 9:
  - What survives: silence detection and render-domain phase continuation machinery.
  - What dies: suppressing decisions in silence as a substitute for a coasting master clock.
  - What changes: locked tempo must continue predicting through sparse/quiet sections with bounded decay/revalidation.

- Requirement 11:
  - What survives: current beat-in-bar and downbeat tick fields.
  - What dies: assuming modulo-4 tick generation equals musical downbeat detection.
  - What changes: downbeat must either be explicitly heuristic, reference-backed, or declared out of V0.

- Requirement 12:
  - What survives: 4/8 bar phrase counters and phrase-progress outputs.
  - What dies: treating phrase progress as full 8/16/32 bar extraction.
  - What changes: phrase-grid scope needs Captain decision because references do not specify a causal algorithm.

- Requirement 13:
  - What survives: current Build/Drop/Breakdown labels as vocabulary and `TranslationEngine` phrase-boundary heuristics as prior art.
  - What dies: bottom-up frame thresholds masquerading as structural section detection.
  - What changes: structural state must be slow, energy-accumulator-driven, and separate from beat phase.

- Requirement 14:
  - What survives: existing intent/action-plan names and the EffectContext-to-Kuramoto parameter path.
  - What dies: direct conflation of song-state classification with immediate effect knobs.
  - What changes: aesthetic intent needs its own state machine and stable parameter surface.

- Requirement 15:
  - What survives: current BPM/phase/tick/confidence fields and effect accessors.
  - What dies: scattering master timing across ESV11 extras, grid snapshots, onset context, and effect helpers without a named contract.
  - What changes: expose one master phase reference with lock/coast/confidence metadata for downstream consumers.

## Part 4 - Open Questions For Captain

1. [FACT] The brief asks for MusicAware over PipelineCore output, but the corrected production target is `esp32dev_audio_esv11_k1v2_32khz`, and current source proves PipelineCore is a separate backend. Should the implementation spec target the ESV11/ControlBus/EffectContext production surface first, or is PipelineCore promotion a separate prerequisite before MusicAware implementation?

2. [FACT] Current `SongAwareDirector` and related handlers/tests are untracked working-tree files, and no local/remote branch or stash in this repo advertises MusicAware/SongAware. Should the next implementation agent treat this untracked working tree as the only source baseline, or should Captain name another fork/clone/stash before specification is finalised?

3. [FACT] Synesthesia Tier 1 docs conflict internally on confidence and octave handling: the app-facing reverse-engineered docs and `synesthesia/` control extraction are not identical. Which Synesthesia doc family wins when the next spec must choose one parameter?

4. [FACT] Tier 1 references do not specify a hard lock/demotion rule. Tier 2 offers conflicting candidates, including Davies 3 consecutive consistent beat-period observations and IBT 5 s induction / 8 bad predictions. Which lock/demotion family should MusicAware adopt?

5. [FACT] Beat tolerance windows disagree across Tier 2 and are absent numerically from Tier 1. Should MusicAware use fixed milliseconds, percent of IBI, or a hybrid tolerance?

6. [FACT] No evidence body provides a complete coast-through-silence and re-validation threshold. What should cause a locked flywheel to demote when audio returns: failed beat predictions, low onset agreement, low energy duration, confidence decay, or a combination?

7. [FACT] Downbeat and phrase-grid extraction are weakly specified by Tier 1 and only partially specified by Tier 2. Should V0 include downbeat/phrase extraction, or should it expose beat-phase lock first and reserve downbeat/phrase for a later phase?

8. [FACT] Current firmware has `SongAwareState::Build/Drop/Breakdown/Dense/Transition`, but no reference provides K1-ready thresholds for verse/chorus/breakdown/build sections. Are those labels intentional K1 visual-language vocabulary to preserve, or accumulated cruft to replace with a smaller structure vocabulary?

9. [FACT] Kuramoto currently consumes `EffectContext` parameters, not a direct director contract. Should MusicAware expose a dedicated Kuramoto parameter surface, or should it continue to feed Kuramoto indirectly through existing speed/intensity/hue/scene fields?

10. [FACT] The current firmware exposes multiple timing surfaces: ESV11 extras, `EsBeatClock` snapshots, `OnsetContext`, and `EffectContext` accessors. Which surface should become the canonical public master phase API for downstream consumers?
