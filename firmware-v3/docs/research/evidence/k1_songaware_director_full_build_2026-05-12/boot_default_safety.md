RBDO label: GROUNDED

Boot/default safety:
- SongAware defaults off.
- Constrained switching defaults false.
- Family morphing defaults false.
- No automatic effect switch on boot.
- Enable grace is 4000 ms.

Serial evidence after upload:
- `enabled=false mode=off`
- `effectiveMode=off owner=none suppressed=disabled`
- `automaticEffectSwitches=0`
- hard health counters all zero.

Policy telemetry:
- `bootGraceMs=3000`
- `postEnableGraceMs=4000`
