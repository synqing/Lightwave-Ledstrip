"""Setup script for lwos-benchmark package."""

from setuptools import find_packages, setup

setup(
    name="lwos-benchmark",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.12",
)
